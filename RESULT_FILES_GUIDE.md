# 실험 결과 파일 가이드

## 📁 파일 위치 구조

10번 파일(`10_analyze_results.bat`) 실행 후 생성되는 모든 결과 파일은 다음 위치에 저장됩니다:

```
runs/
└── 2025-12-13_primary_v1/          # 실험 실행 ID (run_id)
    ├── events.jsonl                # 원본 이벤트 로그 (9번 파일에서 생성)
    ├── run_manifest.json           # 실험 메타데이터 (9번 파일에서 생성)
    ├── resolved_config.json        # 실험 설정 스냅샷 (9번 파일에서 생성)
    ├── summary.json                # 통계 분석 결과 (10번 파일에서 생성) ⭐
    ├── tables/                     # CSV 표 디렉토리 (10번 파일에서 생성) ⭐
    │   ├── overall_comparison.csv
    │   ├── per_turn_comparison.csv
    │   └── efficiency_metrics.csv
    └── figures/                    # 그래프 디렉토리 (10번 파일에서 생성) ⭐
        ├── overall_comparison.png
        ├── overall_comparison.pdf
        ├── per_turn_trends.png
        ├── per_turn_trends.pdf
        ├── efficiency_comparison.png
        ├── efficiency_comparison.pdf
        ├── effect_sizes.png
        └── effect_sizes.pdf
```

---

## 📊 주요 결과 파일 상세 설명

### 1. `summary.json` ⭐ **가장 중요**

**위치**: `runs/2025-12-13_primary_v1/summary.json`

**용도**: 전체 실험 결과의 통계 분석 요약 (JSON 형식)

**주요 내용**:

```json
{
  "schema_version": "summary.v1",
  "run_id": "2025-12-13_primary_v1",
  "created_at_utc": "2025-12-13T...",
  
  "counts": {
    "total_events": 932,           // 총 이벤트 수
    "completed_pairs": 390          // 완료된 페어 수 (LLM + Agent)
  },
  
  "metrics": {
    "by_mode": {
      "llm": {
        "n": 390,
        "metric_rows": [
          {
            "metric": "faithfulness",
            "mean": 0.85,
            "std": 0.12,
            "min": 0.50,
            "max": 1.00
          },
          ...
        ]
      },
      "agent": { ... }
    }
  },
  
  "comparisons": {
    "paired_agent_minus_llm": [
      {
        "metric": "faithfulness",
        "n_pairs": 390,
        "delta_mean": 0.05,          // Agent - LLM 차이
        "delta_std": 0.08,
        "ci95": {
          "low": 0.03,
          "high": 0.07
        },
        "t_test_p_value": 0.001,     // p-value
        "effect_size_cohens_d": 0.42 // Cohen's d
      },
      ...
    ]
  },
  
  "breakdowns": {
    "by_turn": [
      {
        "turn": 1,
        "llm": { "faithfulness": { "mean": 0.82, ... } },
        "agent": { "faithfulness": { "mean": 0.87, ... } }
      },
      ...
    ]
  },
  
  "efficiency": {
    "latency": {
      "by_mode": {
        "llm": {
          "mean": 1234.5,            // 평균 응답 시간 (ms)
          "std": 234.5,
          "min": 800.0,
          "max": 2000.0
        },
        "agent": { ... }
      }
    },
    "cost": {
      "by_mode": {
        "llm": {
          "mean": 0.0015,            // 평균 비용 ($)
          "std": 0.0003
        },
        "agent": { ... }
      }
    },
    "cache": {
      "agent_cache_hit_rate": 0.15   // 캐시 히트율 (15%)
    }
  }
}
```

**논문 작성 시 활용**:
- **통계 검정 결과**: `comparisons.paired_agent_minus_llm`에서 p-value, Cohen's d 추출
  - ⚠️ **참고**: 현재 `events.jsonl`에 메트릭 데이터가 없으면 이 배열이 비어있을 수 있습니다
  - 메트릭 데이터가 있는 경우: faithfulness, answer_relevance 등의 비교 결과 포함
- **효율성 지표**: `efficiency`에서 응답 시간, 비용, 캐시 히트율 확인 ✅ **사용 가능**
  - `efficiency.latency.by_mode.llm.mean`: LLM 평균 응답 시간 (ms) - 예: 8255.4ms
  - `efficiency.latency.by_mode.agent.mean`: Agent 평균 응답 시간 (ms) - 예: 13525.7ms
  - `efficiency.cost.by_mode.llm.mean`: LLM 평균 비용 ($) - 예: $0.000188
  - `efficiency.cost.by_mode.agent.mean`: Agent 평균 비용 ($) - 예: $0.000190
  - `efficiency.cache.agent_cache_hit_rate`: Agent 캐시 히트율 (0.0~1.0) - 예: 0.508 (50.8%)
- **턴별 분석**: `breakdowns.by_turn`에서 턴별 성능 변화 확인
  - ⚠️ **참고**: 메트릭 데이터가 없으면 이 배열이 비어있을 수 있습니다

---

### 2. `tables/overall_comparison.csv` ⭐ **논문 표 1**

**위치**: `runs/2025-12-13_primary_v1/tables/overall_comparison.csv`

**용도**: LLM vs AI Agent 전체 비교 표 (주요 메트릭)

**컬럼 구조**:

| Metric | LLM | AI Agent | Δ (Agent - LLM) | Cohen's d | 95% CI | p-value | Sig. | n |
|--------|-----|----------|-----------------|-----------|--------|---------|------|---|
| Faithfulness | 0.850 ± 0.120 | 0.900 ± 0.100 | +0.050 | 0.42 | [0.03, 0.07] | <0.001 | *** | 390 |
| Answer Relevance | 0.820 ± 0.130 | 0.880 ± 0.110 | +0.060 | 0.48 | [0.04, 0.08] | <0.001 | *** | 390 |
| Context Precision | ... | ... | ... | ... | ... | ... | ... | ... |

**논문 작성 시 활용**:
- **주요 결과 표**: 논문의 Results 섹션에 삽입
- **통계적 유의성**: Sig. 컬럼에서 `***` (p<0.001), `**` (p<0.01), `*` (p<0.05) 확인
- **효과 크기**: Cohen's d 컬럼에서 효과 크기 확인 (Small: <0.5, Medium: 0.5-0.8, Large: ≥0.8)

⚠️ **참고**: 현재 `events.jsonl`에 메트릭 데이터가 없으면 모든 값이 "N/A"로 표시됩니다.
메트릭 데이터가 있는 경우에만 이 표를 사용할 수 있습니다.

---

### 3. `tables/per_turn_comparison.csv` ⭐ **논문 표 2**

**위치**: `runs/2025-12-13_primary_v1/tables/per_turn_comparison.csv`

**용도**: 턴별 성능 비교 표 (멀티턴 대화에서의 성능 변화)

**컬럼 구조**:

| Turn | Metric | LLM | AI Agent | Δ | p-value |
|------|--------|-----|----------|---|---------|
| 1 | Faithfulness | 0.820 | 0.870 | +0.050 | 0.001 |
| 1 | Answer Relevance | 0.800 | 0.860 | +0.060 | <0.001 |
| 2 | Faithfulness | 0.840 | 0.890 | +0.050 | 0.002 |
| ... | ... | ... | ... | ... | ... |
| 5 | Faithfulness | 0.880 | 0.930 | +0.050 | 0.001 |

**논문 작성 시 활용**:
- **턴별 성능 추이**: 멀티턴 대화에서 성능이 어떻게 변화하는지 확인
- **컨텍스트 축적 효과**: 턴이 진행될수록 성능이 향상되는지 확인
- **그래프 생성**: 이 데이터로 `per_turn_trends.png` 그래프 생성

---

### 4. `tables/efficiency_metrics.csv` ⭐ **논문 표 3**

**위치**: `runs/2025-12-13_primary_v1/tables/efficiency_metrics.csv`

**용도**: 효율성 지표 비교 (비용, 응답 시간, 캐시 히트율)

**컬럼 구조**:

| Metric | LLM | AI Agent | Δ (%) |
|--------|-----|----------|-------|
| Cost per turn ($) | 0.0015 | 0.0020 | +33.3% |
| Latency (ms) | 1234.5 | 2345.6 | +90.0% |
| Cache hit rate | 0.0% | 15.0% | +15.0 pp |

**논문 작성 시 활용**: ✅ **이 표는 항상 사용 가능합니다**
- **비용 분석**: AI Agent가 LLM보다 비용이 얼마나 더 드는지 확인
  - 예: LLM $0.000188 vs Agent $0.000190 (약 1% 증가)
- **응답 시간**: AI Agent의 응답 시간이 LLM보다 얼마나 더 걸리는지 확인
  - 예: LLM 8255ms vs Agent 13526ms (약 64% 증가)
  - ⚠️ **주의**: Agent의 응답 시간은 캐시 히트 시 매우 짧아질 수 있음 (median: 73ms)
- **캐시 효과**: 캐시 히트율이 성능에 미치는 영향 분석
  - 예: Agent 캐시 히트율 50.8% (캐시 히트 시 응답 시간이 크게 단축됨)

---

### 5. `figures/overall_comparison.png` ⭐ **논문 그림 1**

**위치**: `runs/2025-12-13_primary_v1/figures/overall_comparison.png`

**용도**: LLM vs AI Agent 전체 비교 막대 그래프

**내용**:
- 주요 메트릭별 막대 그래프 (Faithfulness, Answer Relevance, Context Precision 등)
- 평균값과 표준편차 표시 (에러 바)
- 통계적 유의성 표시 (별표: *, **, ***)
- LLM (파란색) vs AI Agent (빨간색) 비교

**논문 작성 시 활용**:
- **주요 결과 시각화**: 논문의 Results 섹션에 삽입
- **PNG 형식**: Word 문서에 삽입 시 사용
- **PDF 형식**: LaTeX 문서에 삽입 시 사용 (`overall_comparison.pdf`)

---

### 6. `figures/per_turn_trends.png` ⭐ **논문 그림 2**

**위치**: `runs/2025-12-13_primary_v1/figures/per_turn_trends.png`

**용도**: 턴별 성능 추이 선 그래프

**내용**:
- 턴 1~5에 따른 성능 변화 추이
- LLM (파란색 선) vs AI Agent (빨간색 선) 비교
- 주요 메트릭별로 여러 선 그래프 표시

**논문 작성 시 활용**:
- **멀티턴 성능 분석**: 대화가 진행될수록 성능이 어떻게 변화하는지 시각화
- **컨텍스트 축적 효과**: AI Agent가 컨텍스트를 활용하여 성능을 향상시키는지 확인

---

### 7. `figures/efficiency_comparison.png` ⭐ **논문 그림 3**

**위치**: `runs/2025-12-13_primary_v1/figures/efficiency_comparison.png`

**용도**: 효율성 지표 비교 그래프

**내용**:
- 비용 비교 (막대 그래프)
- 응답 시간 비교 (막대 그래프)
- 캐시 히트율 비교 (막대 그래프)

**논문 작성 시 활용**:
- **효율성 분석**: AI Agent의 성능 향상 대비 비용/시간 증가 확인
- **트레이드오프 분석**: 성능 vs 효율성 트레이드오프 논의

---

### 8. `figures/effect_sizes.png` ⭐ **논문 그림 4**

**위치**: `runs/2025-12-13_primary_v1/figures/effect_sizes.png`

**용도**: Cohen's d 효과 크기 시각화

**내용**:
- 각 메트릭별 Cohen's d 값 (수평 막대 그래프)
- 효과 크기 색상 코딩:
  - **Small** (|d| < 0.5): 회색
  - **Medium** (0.5 ≤ |d| < 0.8): 주황색
  - **Large** (|d| ≥ 0.8): 빨간색
- 효과 크기 임계값 표시 (0.5, 0.8)

**논문 작성 시 활용**:
- **효과 크기 분석**: 통계적 유의성뿐만 아니라 실질적 효과 크기 확인
- **실용적 의미**: p-value가 작아도 효과 크기가 작으면 실용적 의미가 제한적일 수 있음

---

## 📝 논문 작성 워크플로우

### 1단계: `summary.json` 검토

```bash
# 주요 통계 확인
cat runs/2025-12-13_primary_v1/summary.json | python -m json.tool
```

**확인 사항**:
- 총 이벤트 수 (`counts.total_events`)
- 완료된 페어 수 (`counts.completed_pairs`)
- 주요 메트릭의 평균값과 표준편차
- p-value와 Cohen's d 값

---

### 2단계: CSV 표 검토 및 논문 삽입

**표 1: 전체 비교** (`overall_comparison.csv`)
- 논문의 Results 섹션에 삽입
- 주요 메트릭별 LLM vs AI Agent 비교
- 통계적 유의성 표시

**표 2: 턴별 비교** (`per_turn_comparison.csv`)
- 멀티턴 대화에서의 성능 변화 분석
- 턴별 성능 추이 확인

**표 3: 효율성 지표** (`efficiency_metrics.csv`)
- 비용, 응답 시간, 캐시 히트율 비교
- 트레이드오프 분석

---

### 3단계: 그래프 검토 및 논문 삽입

**그림 1: 전체 비교** (`overall_comparison.png`)
- 주요 결과 시각화
- Word 문서: PNG 사용
- LaTeX 문서: PDF 사용

**그림 2: 턴별 추이** (`per_turn_trends.png`)
- 멀티턴 성능 변화 시각화

**그림 3: 효율성 비교** (`efficiency_comparison.png`)
- 비용/시간 비교 시각화

**그림 4: 효과 크기** (`effect_sizes.png`)
- Cohen's d 효과 크기 시각화

---

### 4단계: 통계 검정 결과 작성

`summary.json`의 `comparisons.paired_agent_minus_llm`에서 다음 정보 추출:

```python
# 예시: Faithfulness 메트릭
{
  "metric": "faithfulness",
  "n_pairs": 390,
  "delta_mean": 0.05,          # Agent가 LLM보다 0.05 높음
  "delta_std": 0.08,
  "ci95": {"low": 0.03, "high": 0.07},  # 95% 신뢰구간
  "t_test_p_value": 0.001,     # p < 0.001 (매우 유의함)
  "effect_size_cohens_d": 0.42 # 중간 효과 크기
}
```

**논문 작성 예시**:
> "AI Agent 모드는 LLM 모드에 비해 Faithfulness가 평균 0.05 높았으며 (95% CI: [0.03, 0.07]), 
> 이 차이는 통계적으로 매우 유의하였다 (paired t-test, p < 0.001, Cohen's d = 0.42, 중간 효과 크기)."

---

## 🔍 파일 접근 방법

### Windows 탐색기에서

```
C:\Users\KHIDI\Downloads\medical_ai_agent_minimal\runs\2025-12-13_primary_v1\
```

### 명령줄에서

```bash
# CSV 표 확인
cd runs/2025-12-13_primary_v1/tables
dir *.csv

# 그래프 확인
cd ../figures
dir *.png

# summary.json 확인
cd ..
type summary.json
```

### Python에서

```python
import json
import pandas as pd

# summary.json 읽기
with open('runs/2025-12-13_primary_v1/summary.json', 'r', encoding='utf-8') as f:
    summary = json.load(f)

# CSV 표 읽기
df_overall = pd.read_csv('runs/2025-12-13_primary_v1/tables/overall_comparison.csv')
df_per_turn = pd.read_csv('runs/2025-12-13_primary_v1/tables/per_turn_comparison.csv')
df_efficiency = pd.read_csv('runs/2025-12-13_primary_v1/tables/efficiency_metrics.csv')
```

---

## ✅ 체크리스트

논문 작성 전 확인 사항:

- [ ] `summary.json`에서 주요 통계 확인
- [ ] `overall_comparison.csv`에서 주요 메트릭 확인
- [ ] `per_turn_comparison.csv`에서 턴별 성능 추이 확인
- [ ] `efficiency_metrics.csv`에서 효율성 지표 확인
- [ ] 모든 그래프 파일이 생성되었는지 확인
- [ ] p-value와 Cohen's d 값이 논문에 포함되었는지 확인
- [ ] 95% 신뢰구간이 논문에 포함되었는지 확인

---

## 📌 참고사항

1. **파일 형식**:
   - CSV 파일: UTF-8 인코딩, Excel에서 바로 열 수 있음
   - PNG 파일: 300 DPI 고해상도, Word 문서에 적합
   - PDF 파일: 벡터 형식, LaTeX 문서에 적합

2. **통계 검정**:
   - Paired t-test 사용 (LLM과 Agent가 같은 환자/턴/질문에 대해 비교)
   - Cohen's d 효과 크기: Small (<0.5), Medium (0.5-0.8), Large (≥0.8)
   - 유의수준: p < 0.05 (*), p < 0.01 (**), p < 0.001 (***)

3. **논문 작성 팁**:
   - 표와 그래프는 상호 보완적: 표는 정확한 수치, 그래프는 시각적 비교
   - 통계 검정 결과는 표와 본문 모두에 포함
   - 효과 크기는 p-value와 함께 보고 (통계적 유의성 + 실질적 의미)

---

이 가이드를 참고하여 논문을 작성하시면 됩니다! 🎓

